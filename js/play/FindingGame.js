import React, { Component } from 'react';
import { Container, Icon, Content, List, ListItem, Text, Right, Left } from 'native-base';

export default class FindingGame extends Component {
  static navigationOptions = {
    title: 'Finding Game'
  };

  render() {
    return (
      <Container style={{backgroundColor: 'white'}}>
        <Content>
        </Content>
      </Container>
    );
  }
}
